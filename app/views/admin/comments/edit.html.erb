<%= error_messages_for :comment %>

<% form_for(:comment, :action => url(:edit_comment,@comment)) do |f| %>
        <p>
                <%= text_area_control :body, :label => "Body", :size => "40x8" %>
        </p>
        <p>
                <button type="submit" class="button positive">
            <%= image_tag "icons/tick.png" %> Change
          </button>
        </p>
<% end %>

<div class="buttons">
  <a href="<%= url(:comments) %>" class="button">
        <%= image_tag "icons/arrow_undo.png" %>Back
  </a>
</div>
